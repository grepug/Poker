{
  "name": "poker-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:registry": "node ./scripts/build-registry.mjs",
    "start": "pnpm run build:registry && node ./src/server.mjs",
    "dev": "pnpm run build:registry && node --watch ./src/server.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "fastify": "^5.6.1"
  }
}
